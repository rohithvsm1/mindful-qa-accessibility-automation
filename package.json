{
  "name": "mindful-qa-accessibility-automation",
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test",
    "test:ci": "playwright test --project=chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "@playwright/test": "^1.47.0"
  }
}
